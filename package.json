{
  "name": "sift-cloudflare",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that fronts the Sift containers (Django + Baileys sidecar).",
  "scripts": {
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "pages:deploy": "cd frontend && npm run build && wrangler pages deploy dist --project-name sift"
  },
  "devDependencies": {
    "wrangler": "^4.110.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  }
}
